<UserControl x:Class="PrimalEngineEditor.AllEditors.GeometryEditorWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ed="clr-namespace:PrimalEngineEditor.AllEditors"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=ed:GeometryEditor}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource LightTextBlockStyle}"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <ed:GeometryEditor/>
    </UserControl.DataContext>

    <DockPanel Background="{StaticResource Editor.Window.GrayBrush2}">
        <!-- Neon Border -->
        <Border Style="{StaticResource NeonCard}"
            Height="40" DockPanel.Dock="Top"
            VerticalAlignment="Top" Margin="0,0,0,1">

            <StackPanel Orientation="Horizontal" Margin="5,2">
                <TextBlock Text="LOD:" Margin="0,0,5,0"/>
                <!-- Neon Slider -->
                <Slider Width="130" VerticalAlignment="Center" 
                    x:Name="lodSlider"
                    Style="{StaticResource BlueSlider}"/>
                <!-- Neon TextBox -->
                <TextBox Width="32" 
                     Text="{Binding Path=Value, ElementName=lodSlider}"
                     Style="{StaticResource NeonTextBox}"/>
                <TextBlock Text="Auto LOD" Margin="15,0,5,0"/>
                <CheckBox IsChecked="False" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="250" MaxWidth="400"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- GridSplitter -->
            <GridSplitter HorizontalAlignment="Right" Width="5"
                      Background="{StaticResource Editor.Window.GrayBrush1}"
                      VerticalAlignment="Stretch"/>

            <ed:GeometryWindow DataContext="{Binding MeshRenderer}" 
                         Grid.Column="1" 
                         x:Name="geometryView"/>
            
            <StackPanel Grid.Column="1" Margin="5" VerticalAlignment="Top" HorizontalAlignment="Left" IsHitTestVisible="False">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding MeshRenderer.CameraPosition.X, StringFormat=G5}" Foreground="{DynamicResource Editor.RedBrush}"/>
                    <TextBlock Text="{Binding MeshRenderer.CameraPosition.Y, StringFormat=G5}" Margin="10,0" Foreground="{DynamicResource Editor.GreenBrush}"/>
                    <TextBlock Text="{Binding MeshRenderer.CameraPosition.Z, StringFormat=G5}" Foreground="{DynamicResource Editor.BlueBrush}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding MeshRenderer.Meshes[0].Positions.Count}" Foreground="{DynamicResource Editor.OrangeBrush}"/>
                    <TextBlock Text="{Binding MeshRenderer.Meshes[0].Indices.Count}" Margin="10,0" Foreground="{DynamicResource Editor.OrangeBrush}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </DockPanel>

</UserControl>
