<Window x:Class="PrimalEngineEditor.Content.PrimitiveMeshDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrimalEngineEditor.Content"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:utl="clr-namespace:PrimalEngineEditor.Utilities.Controls"
        mc:Ignorable="d" ResizeMode="NoResize"
        Foreground="{DynamicResource Editor.FontBrush}"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True" 
        Title="Primitive Mesh" Height="600" Width="800"
        Style="{StaticResource AgilisDialogStyle}">

    <Window.Resources>
        <ObjectDataProvider x:Key="dataFromEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:PrimitiveMeshType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <DockPanel Background="{DynamicResource Editor.Window.GrayBrush2 }">
        <Border Background="{DynamicResource Editor.Window.GrayBrush3}" Height="32" DockPanel.Dock="Top"
                VerticalAlignment="Top" Margin="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect Opacity="0.3" BlurRadius="10" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" Margin="5,0">
                <ComboBox SelectedIndex="0" Width="100" VerticalAlignment="Center" 
                          ItemsSource="{Binding Source={StaticResource dataFromEnum}}" x:Name="primTypeComboBox"
                          SelectionChanged="OnPrimitiveType_ComboBox_SelectionChanged"/>
            </StackPanel> 
        </Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="100" MaxWidth="200"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!--  PLANE SETTINGS -->
            <StackPanel Background="#0B0F1A">
                <StackPanel.Resources>

                    <!-- Neon Card Style -->
                    <Style TargetType="Border" x:Key="NeonCard">
                        <Setter Property="CornerRadius" Value="10"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="BorderBrush" Value="#2A2D36"/>
                        <Setter Property="Margin" Value="0,10"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Background" Value="#121827"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="0" ShadowDepth="0" Color="{DynamicResource Editor.CyanColor}" Opacity="0"/>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#00FFFF"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="{DynamicResource Editor.CyanColor}" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Blue Square Thumb -->
                    <Style x:Key="BlueSquareThumb" TargetType="Thumb">
                        <Setter Property="Width" Value="14"/>
                        <Setter Property="Height" Value="14"/>
                        <Setter Property="Background" Value="{DynamicResource Editor.BlueBrush}"/>
                        <Setter Property="BorderBrush" Value="#A6F6FF"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="0" ShadowDepth="0" Color="{DynamicResource Editor.CyanColor}" Opacity="0"/>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="18" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Height" To="18" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Effect.BlurRadius" To="12" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.6" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="14" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Height" To="14" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Effect.BlurRadius" To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Slider Style -->
                    <Style TargetType="Slider" x:Key="BlueSlider">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Slider">
                                    <Grid Margin="5,0">
                                        <Track x:Name="PART_Track">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Background="{DynamicResource Editor.Window.GrayBrush1}" Height="4" Command="Slider.DecreaseLarge"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Background="{DynamicResource Editor.Window.GrayBrush1}" Height="4" Command="Slider.IncreaseLarge"/>
                                            </Track.IncreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Style="{StaticResource BlueSquareThumb}" />
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- Save Button Neon Style -->
                    <Style TargetType="Button" x:Key="NeonButton">
                        <Setter Property="Background" Value="#121827"/>
                        <Setter Property="Foreground" Value="#A6F6FF"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="BorderBrush" Value="#2A2D36"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="0" ShadowDepth="0" Color="{DynamicResource Editor.CyanColor}" Opacity="0"/>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="10" 
                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#00FFFF"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="{DynamicResource Editor.CyanColor}" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                </StackPanel.Resources>

                <!-- Segments -->
                <Border Style="{StaticResource NeonCard}">
                    <StackPanel>
                        <TextBlock Text="Segments" Foreground="#A6F6FF" FontWeight="Bold" FontSize="14"/>
                        <DockPanel Margin="5">
                            <TextBlock Text="Width" Width="60" Foreground="#66FFFF"/>
                            <TextBox Text="{Binding Path=Value, ElementName=xSliderPlane}" Width="40" Margin="10,0"/>
                            <Slider x:Name="xSliderPlane" Minimum="1" Maximum="10" TickFrequency="0.1"
                                    IsSnapToTickEnabled="False" ValueChanged="OnSlider_ValueChanged"
                                    Style="{StaticResource BlueSlider}"/>
                        </DockPanel>
                        <DockPanel Margin="5">
                            <TextBlock Text="Length" Width="60" Foreground="#66FFFF"/>
                            <TextBox Text="{Binding Path=Value, ElementName=zSliderPlane}" Width="40" Margin="10,0"/>
                            <Slider x:Name="zSliderPlane" Minimum="1" Maximum="10" TickFrequency="0.1"
                                    IsSnapToTickEnabled="False" ValueChanged="OnSlider_ValueChanged"
                                    Style="{StaticResource BlueSlider}"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <!-- Size -->
                <Border Style="{StaticResource NeonCard}">
                    <StackPanel>
                        <TextBlock Text="Size" Foreground="#A6F6FF" FontWeight="Bold" FontSize="14"/>
                        <DockPanel Margin="5">
                            <TextBlock Text="Width" Width="60" Foreground="#66FFFF"/>
                            <utl:ScalarBox x:Name="widthScalarBoxPlane" Value="1" ValueChanged="OnScalarBox_ValueChanged"/>
                        </DockPanel>
                        <DockPanel Margin="5">
                            <TextBlock Text="Length" Width="60" Foreground="#66FFFF"/>
                            <utl:ScalarBox x:Name="lengthScalarBoxPlane" Value="1" ValueChanged="OnScalarBox_ValueChanged"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <!-- Save Button -->
                <Button Content="Save" Width="96" Margin="5,0" Style="{StaticResource NeonButton}"/>
            </StackPanel>
            <!--  CUBE SETTINGS -->
            <!--  UVSPHERE SETTINGS -->
            <!--  ICOSPHERE SETTINGS -->
            <!--  CYLINDER SETTINGS -->
            <!--  CAPSULE SETTINGS -->
        </Grid>
    </DockPanel>
</Window>
